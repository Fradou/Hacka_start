<div class="container">
    <div class="row">
        <div class="col-md-5  toppad  pull-right col-md-offset-3 ">
            <A href="{{ path('fos_user_security_logout') }}" >Logout</A>
            <p class=" text-info">{{ "now"|date("m/d/Y") }}</p>
        </div>
        <div class="col-xs-12 col-sm-12 col-md-6 col-lg-6 col-xs-offset-0 col-sm-offset-0 col-md-offset-3 col-lg-offset-3 toppad" >


            <div class="panel panel-info">
                <div class="panel-heading">
                    <h3 class="panel-title">{{ user.firstname }} {{ user.lastname }} <span class="text-info pull-right">{{ "now"|date("m/d/Y") }}</span></h3>
                </div>
                <div class="panel-body">
                    <div class="row">
                        <div class="col-md-3 col-lg-3 " align="center"> <img alt="User Pic" src="{{ asset('assets/img/'~user.profilepicture) }}" class="img-circle img-responsive"> </div>
                        <div class=" col-md-9 col-lg-9 ">
                            <table class="table table-user-information">
                                <tbody>
                                <tr>
                                    <td>User Name</td>
                                    <td><p>{{ user.username }}</p></td>
                                </tr>
                                <tr>
                                    <td>Nationality :</td>
                                    <td><p>{{ user.nationality }}</p></td>
                                </tr>
                                <tr>
                                    <td>Native Language</td>
                                    <td><p>{{ user.nativelanguage }}</p></td>
                                </tr>
                                <tr>
                                    <td>Paypal Account</td>
                                    <td>{{ user.paypalaccount }}</td>
                                </tr>
                                <tr>
                                <tr>
                                    <td>Are you translator ?</td>
                                    <td><p>{{ user.translator }}</p></td>
                                </tr>
                                <tr>
                                    <td>Home Address</td>
                                    <td>Kathmandu,Nepal</td>
                                </tr>
                                <tr>
                                    <td>Email</td>
                                    <td>{{ user.email }}</a></td>
                                </tr>
                                <td>Phone Number</td>
                                    <td>555-4567-890(Mobile)</td>
                                </tr>
                                </tbody>
                            </table>
                            <a href="mailto:'.{{ user.email }}.'" data-original-title="Broadcast Message" data-toggle="tooltip" type="button" class="btn btn-sm btn-primary"><i class="glyphicon glyphicon-envelope"></i></a>
                            <span class="pull-right">
                                <a href="{{ path('fos_user_profile_edit') }}" data-original-title="Edit this user" data-toggle="tooltip" type="button" class="btn btn-sm btn-warning"><i class="glyphicon glyphicon-edit"></i></a>
                            </span>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>